import { useCallback } from 'react';

const usePlaylistSongsMapper = () => {
    const mapPlaylistSongs = useCallback(() => {

    }, []);

    return { mapPlaylistSongs };
};

export default usePlaylistSongsMapper;